<Window
    x:Class="SystemOptimizer.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Windows Optimizer"
    Width="800" Height="600"
    WindowStartupLocation="CenterScreen"
    xmlns:converters="clr-namespace:SystemOptimizer.Converters">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:StringVisibilityConverter x:Key="StringVisibilityConverter" />
        
        <!-- Styles -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#106EBE"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#444"/>
                    <Setter Property="Foreground" Value="#888"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#444"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#222"/>
                    <Setter Property="Foreground" Value="#555"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource SecondaryButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#C50F1F"/>
                    <Setter Property="BorderBrush" Value="#C50F1F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#202020">
        <Grid Margin="32">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,24">
                <TextBlock Text="Windows Optimizer" FontSize="32" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="Optimize &amp; Clean Windows System" FontSize="14" Foreground="#AAAAAA" Margin="0,4,0,0"/>
            </StackPanel>

            <!-- Stats -->
            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Style="{StaticResource CardStyle}" Margin="0,0,12,16">
                    <StackPanel>
                        <TextBlock Text="Trash capacity" FontSize="13" Foreground="#AAAAAA"/>
                        <TextBlock Text="{Binding TotalTrashDisplay}" FontSize="28" FontWeight="Bold" Foreground="#0078D4" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="1" Style="{StaticResource CardStyle}" Margin="12,0,12,16">
                    <StackPanel>
                        <TextBlock Text="Number of files found" FontSize="13" Foreground="#AAAAAA"/>
                        <TextBlock Text="{Binding TotalFileCount}" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
                
                <Border Grid.Column="2" Style="{StaticResource CardStyle}" Margin="12,0,0,16" 
                        Visibility="{Binding FreedDisplay, Converter={StaticResource StringVisibilityConverter}, FallbackValue=Collapsed}">
                    <StackPanel>
                        <TextBlock Text="Freed space" FontSize="13" Foreground="#AAAAAA"/>
                        <TextBlock Text="{Binding FreedDisplay}" FontSize="28" FontWeight="Bold" Foreground="#107C10" Margin="0,4,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Logs / Status Area -->
            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" 
                               FontSize="14" FontWeight="SemiBold" Foreground="White" Margin="0,0,0,12"/>
                               
                    <ListBox Grid.Row="1" ItemsSource="{Binding Logs}" 
                             Background="#1A1A1A" Foreground="#CCCCCC"
                             BorderThickness="1" BorderBrush="#333"
                             FontFamily="Consolas" FontSize="12"
                             VirtualizingPanel.IsVirtualizing="True"
                             VirtualizingPanel.VirtualizationMode="Recycling"
                             Padding="8" Margin="0,4,0,0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <ContentPresenter Margin="0,2"/>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    
                    <Border Grid.Row="1" Background="#B31A1A1A" CornerRadius="4" 
                            Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="O" FontFamily="Consolas" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8">
                                <TextBlock.RenderTransform>
                                    <RotateTransform Angle="0" CenterX="16" CenterY="16"/>
                                </TextBlock.RenderTransform>
                            </TextBlock>
                            <TextBlock Text="Scanning..." FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Grid.Row="1" Background="#B31A1A1A" CornerRadius="4" 
                            Visibility="{Binding IsCleaning, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="X" FontFamily="Consolas" FontSize="32" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="Cleaning..." FontSize="14" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="Cancel Scan" Command="{Binding CancelCommand}" Style="{StaticResource DangerButton}"
                        Visibility="{Binding IsScanning, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,12,0"/>
                <Button Content="Cancel Cleaning" Command="{Binding CancelCommand}" Style="{StaticResource DangerButton}"
                        Visibility="{Binding IsCleaning, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Margin="0,0,12,0"/>

                <Button Content="Scan" Command="{Binding ScanCommand}" Style="{StaticResource PrimaryButton}" Margin="0,0,12,0"/>
                <Button Content="Clean" Command="{Binding CleanCommand}" Style="{StaticResource SecondaryButton}"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
